package chapter2;

/**
 * p,q指针使得a[lo,p-1]以及a[q+1,hi]中的元素都和a[lo]相等
 * 指针i：使得a[p,i-1]小于a[lo];
 * 指针j：使得a[j+1,q]大于a[lo];
 */
public class QuickSort3Way {
    private static void sort(Comparable[] a, int lo, int hi){
        if (hi <= lo){
            return;
        }
    }
}
